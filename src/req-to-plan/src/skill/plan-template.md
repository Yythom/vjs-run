# <需求名称>

> 需求池: `<record_id>` ｜ 业务线: `<video/foto/...>` ｜ 状态: 排实施
> 方案: <方案链接>
> 视觉: <视觉稿链接，无则写「无」>

## 1. 需求要点

<用 3-5 条写清楚要做什么。只写实施相关的，跳过背景与复盘。>

从需求文档「检查」段提取的连带项：

| 项 | 内容 | 涉及改动 |
| --- | --- | --- |
| 数据埋点 | <埋点要求> | <文件或「无」> |
| 消息更新同步 | <站内信/短信/邮件> | <文件或「无」> |
| 后台权限配置 | <是/否> | <说明> |

## 2. 影响面

### 2.1 要新建（来源: `impact-scan mirror`）

| 目标文件 | 参照实现 | 可机械复制 |
| --- | --- | --- |
| `<path>` | `<source path>` | 是 / 否（说明差异） |

### 2.2 要修改（来源: `impact-scan locate`）

| 文件 | 孪生数 | 改动说明 |
| --- | --- | --- |
| `<app 内相对路径>` | `N 个 app` | <说明> |

> 孪生数 > 1 的，下面列全部路径，逐一改，不要只改一处：
>
> - `apps/<a>/<path>`
> - `apps/<b>/<path>`

### 2.3 检索记录

| 关键词 | scope | 命中 |
| --- | --- | --- |
| `<词>` | `<前缀>` | `<n>` |

<记下命中 0 或命中明显跑偏的词，说明换成了什么。这是给复盘用的。>

## 3. 接口对账

| 用途 | 接口 | 状态 |
| --- | --- | --- |
| <读/写什么> | `<path>` | 已有 / 字段不足 / **不存在，需提需求** |

## 4. 需要产品澄清

<需求文档里自标的「开发阶段确认」项 + 实施必需但文档没写的。每条要能被一句话回答。>

1. <问题> —— 来源: 需求文档「<段落>」
2. <问题> —— 来源: 实施推导
3. <问题> —— 来源: UI 稿（图 `assets/img-NN.png`），需确认是否为最新

## 5. 分诊结论

**可自动实施：** <条目，或「无」>

**需人工：** <条目及理由>

**阻塞：** <接口未就绪等，或「无」>

## 6. 实施顺序

1. <先做什么，为什么>
2. ...

## 7. 验证

```bash
pnpm typecheck:tsgo --filter=<workspace>
pnpm lint
```

<补充本需求特有的验证点：要点开的页面、要走通的流程。>
