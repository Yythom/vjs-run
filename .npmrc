enable-pre-post-scripts=true
# 扁平化 node_modules（npm 风格，无符号链接）。
# electron-builder 打包 pnpm 项目时，默认的 .pnpm 符号链接结构会漏掉传递依赖
# （如 strip-ansi 的 ansi-regex、http-proxy 的 follow-redirects），导致打包后 ERR_MODULE_NOT_FOUND。
node-linker=hoisted
