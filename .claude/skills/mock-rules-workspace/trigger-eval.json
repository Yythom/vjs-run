[
  {
    "query": "登录那个接口后端还没好，我先把 /api/login 的返回写死一下，返回 token 是啥都行，我这边先把跳转逻辑调通",
    "should_trigger": true,
    "note": "定制-基础，没说\"mock 规则\"四个字"
  },
  {
    "query": "订单列表要调分页，能不能让 page=1 返回 10 条、page=2 返回 3 条、page=3 直接空数组？我想试试触底加载的边界",
    "should_trigger": true,
    "note": "定制-变体，按 query 条件返回不同响应"
  },
  {
    "query": "产品说要给慢网络加个 loading 骨架屏，我本地接口秒回根本看不到效果，有办法让 /api/dashboard/summary 慢个三四秒吗",
    "should_trigger": true,
    "note": "定制-delay，用户描述的是需求不是手段"
  },
  {
    "query": "我刚把 mock-rules.json 里 /api/user/profile 的 response 改了，保存了也没重启，前端拿到的还是老数据，是不是要重启 vjtools",
    "should_trigger": true,
    "note": "排障-规则不生效（新增能力）"
  },
  {
    "query": "奇怪 我明明写的 response 是个数组 [{id:1},{id:2}]，前端 console 打出来变成了 {rc:0,code:'SUCCESS',data:[...]}，这层 rc 是谁给我包上去的？",
    "should_trigger": true,
    "note": "排障-信封化。完全没提 mock，最能测描述的 pushy 程度"
  },
  {
    "query": "请求 /api/report/export 一直报 502，body 里写着 No mock route matched 之类的，但我 swagger 里明明有这个路径啊",
    "should_trigger": true,
    "note": "排障-502/404 分支"
  },
  {
    "query": "怎么判断我这个请求到底走的是假数据还是真的打到测试环境后端了？两边返回长得差不多我有点分不清",
    "should_trigger": true,
    "note": "排障-x-mock-proxy"
  },
  {
    "query": "上周做支付联调那套假数据我还想再用一次，当时好像存过一个场景，但现在活动规则里是别的东西了，怎么切回去",
    "should_trigger": true,
    "note": "定制-场景切换"
  },
  {
    "query": "变体我按文档加了个 when-body status=2 的，name 叫已完成订单，但请求发过去永远返回的是兜底那份，条件我核对好几遍没写错",
    "should_trigger": true,
    "note": "排障-变体不命中（含静默丢弃这个隐藏原因）"
  },
  {
    "query": "把 /api/message/unread 那条先停掉别让它拦截，我要看真实后端返回什么，但别删，等会还要用",
    "should_trigger": true,
    "note": "定制-disable"
  },

  {
    "query": "renderer 里 MockConfigPanel.tsx 这个组件太长了想拆一下，规则列表那块抽成子组件，顺便把 useState 换成 RHF",
    "should_trigger": false,
    "note": "近似-改的是 vjtools 里 mock 配置页的前端代码，不是 mock 数据"
  },
  {
    "query": "src/mock/server.js 里我想加个能力：规则支持正则匹配 path，不只是 {id} 占位符，帮我看看 findRoute 那块怎么改",
    "should_trigger": false,
    "note": "最强近似-改 mock server 实现而非用它造数据"
  },
  {
    "query": "我们组件库的单测里 vi.mock('@/api/user') 之后 getUserInfo 还是走了真实实现，是不是 hoisting 的问题",
    "should_trigger": false,
    "note": "近似-vitest 的 mock，同词不同域"
  },
  {
    "query": "配置里 backendBaseUrl 我填的是测试环境那个地址，但想再加一个预发的，能不能支持多个后端按项目切换",
    "should_trigger": false,
    "note": "近似-backendBaseUrl 是 mock server 的配置项且 skill 里提过，但这是要改 vjtools 的配置能力"
  },
  {
    "query": "后端给的 swagger.json 里 UserProfile 这个 schema 少了 avatarUrl 字段，我改一下本地这份 spec 文件让它能生成出来",
    "should_trigger": false,
    "note": "近似-改 spec 源文件，skill 明确不管这个"
  },
  {
    "query": "3002 端口被占了 vjtools 起不来，报 EADDRINUSE，能帮我看下是哪个进程吗",
    "should_trigger": false,
    "note": "近似-正是 mockPort 但问题是端口占用"
  },
  {
    "query": "e2e 那个 case 需要数据库里先有 50 条已支付订单，帮我写个 seed 脚本塞进去",
    "should_trigger": false,
    "note": "近似-造假数据但目标是 DB 不是接口"
  },
  {
    "query": "线上 /api/order/detail 返回的 amount 是分为单位，前端展示成了 12300 元，这个得后端改还是我这边除 100",
    "should_trigger": false,
    "note": "近似-真实接口的业务问题，不涉及 mock"
  },
  {
    "query": "想给项目引入 msw，本地开发和单测共用一套 handler，你觉得跟现在这套方案冲突吗值得弄吗",
    "should_trigger": false,
    "note": "近似-引入别的 mock 方案，是技术选型讨论"
  },
  {
    "query": "帮我把 mock-rules.json 这个文件加到 gitignore 里，另外看看 .gitignore 里还有哪些是重复的",
    "should_trigger": false,
    "note": "最强近似-文件名精确命中但任务是 git 配置"
  }
]
